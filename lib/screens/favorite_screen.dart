import 'package:flutter/material.dart';

class FavoriteSreen extends StatelessWidget {
  const FavoriteSreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('favorita'),
    );
  }
}
